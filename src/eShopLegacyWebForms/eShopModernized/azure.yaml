# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: eshop-modernized
metadata:
  template: eshop-modernized@0.0.1

services:
  web:
    project: .
    language: csharp
    host: appservice
    
infra:
  provider: terraform
  path: infra
